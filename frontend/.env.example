# Relative URL so Vite (dev) and nginx (Docker) can proxy /api to the backend.
VITE_API_BASE_URL=/api/v1
# Optional. Defaults to ws(s)://{current host}/api/v1
# VITE_WS_URL=ws://localhost:8000/api/v1
